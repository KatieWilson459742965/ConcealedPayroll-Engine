{
  "name": "concealed-payroll-engine",
  "version": "1.1.0",
  "description": "Privacy-preserving payroll management system with Zama FHE (fhEVM 0.9.1)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@fhevm/hardhat-plugin": "^0.3.0-1",
    "@fhevm/solidity": "^0.9.1",
    "dotenv": "^16.4.0"
  }
}
